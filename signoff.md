Gap-closure judgment: Closed. I now understand, in a way I did not before, that my Conversion Engine funnel is **scaffold-first**: **`tool_calls`** in **`agent_trace_log.jsonl`** often summarize **what Python ran**, not proof that the model **chose** among a full tool menu in one completion—so “bad agent” was the wrong default story. I understand how to separate **(1) model-emitted** actions (real **`tool_calls`** from the provider), **(2) tool-schema / description** effects (what was **`available`**, **`tool_schema_version`**), and **(3) scaffold** (state, preconditions, **`blocked_tools`** / reason codes)—and that **attribution** means logging **decisions at the layer they were made**, with **safe summaries** instead of raw reasoning or unnecessary PII. My **grounding commit** is to extend **`StructuredLogger.emit`** and the **`leads_router` / orchestrator** call sites with those fields so traces stay honest as I pilot real **MCP / function-calling** on selected stages. That closes the Day 2 gap in my research question.
