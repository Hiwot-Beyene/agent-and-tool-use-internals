On the morning call we walked the Day 2 topic bank (**agent and tool-use internals**—function calling, MCP surfaces, scaffold vs model, multi-turn planning) and agreed to **ground everything in our Week 10 `conversion-engine` work** rather than abstract agent theory. My first draft blurred “the model picked the wrong tool” with **code-scheduled stages** (`enrich → compose → send → reply → book`) and **post-hoc `tool_calls`** rows that often summarize Python substeps, not provider-native routing. We sharpened the question to: **how to design traces and (eventually) MCP tool defs** so I can separate **model-emitted choice**, **tool-schema effects**, and **scaffold**—and what to log vs withhold (**no raw reasoning**, minimal PII). We scoped **out** a full autonomous multi-tool replan for this day; the explainer should stay one coherent piece focused on **attribution fields**, **honest labeling** of scaffold-only steps, and a **practical next edit** to `StructuredLogger` / router traces—not a rewrite of the whole funnel.
